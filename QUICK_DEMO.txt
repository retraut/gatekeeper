╔═══════════════════════════════════════════════════════╗
║     GATEKEEPER - Quick Demo (No Errors!)             ║
╚═══════════════════════════════════════════════════════╝

🎯 СЦЕНАРІЙ 1: Тільки Go CLI (5 хвилин)
────────────────────────────────────────────────────────

$ ./build.sh --cli --install

✅ Результат:
  • Binary: ~/.local/bin/gatekeeper
  • Config: ~/.config/gatekeeper/config.yaml
  • Helper: ~/.local/bin/gatekeeper-tmux
  • Status: ✅ All installed

$ gatekeeper status
AWS: ❌ dead
GitHub: ✅ alive


🎯 СЦЕНАРІЙ 2: З macOS App + Widget
────────────────────────────────────────────────────────

Без Xcode:
$ ./build.sh --app

✅ Результат:
  ℹ️  Clear step-by-step instructions
  • Install Xcode
  • Open project
  • Build in Xcode
  • Run & add widgets


З Xcode (після встановлення):
$ ./build.sh --app

✅ Результат:
  ✅ Built Gatekeeper.app
  • Opens folder with app
  • Ready to launch


🎯 СЦЕНАРІЙ 3: Clean Rebuild
────────────────────────────────────────────────────────

$ ./build.sh --clean --cli

✅ Результат:
  ✅ Cleaned build artifacts
  ✅ Built gatekeeper (5.9M)
  ✅ Installed to ~/.local/bin


🎯 СЦЕНАРІЙ 4: Перевірка
────────────────────────────────────────────────────────

$ ./build.sh --test

✅ Результат:
  ✅ Binary installed at: ~/.local/bin/gatekeeper
  ✅ Config created at: ~/.config/gatekeeper/config.yaml
  ✅ tmux helper installed at: ~/.local/bin/gatekeeper-tmux
  
  Next steps:
  1. Edit config: nano ~/.config/gatekeeper/config.yaml
  2. Start daemon: gatekeeper daemon
  3. Check status: gatekeeper status --compact


✨ КЛЮЧОВІ МОМЕНТИ
════════════════════════════════════════════════════════

✅ Немає помилок про xcodebuild
✅ Чіткі інструкції замість помилок
✅ Слідити за "Next steps"
✅ Простий одна-команда білда

Більше: ./build.sh --help
