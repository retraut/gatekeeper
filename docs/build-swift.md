# Swift Build Options

## 🤔 Проблема

macOS app (MenuBar + WidgetKit) потребує:
- Повна Xcode (~15GB)
- Xcode SDK (не є в CLI Tools)
- `.xcodeproj` файли

## ✅ Рішення

### Опція 1: Використовуй Go CLI (Рекомендується)

Go CLI робить усе що потрібно і біжить навіть в tmux/headless:

```bash
./build.sh --cli
gatekeeper daemon
gatekeeper status --compact  # для tmux
```

**Переваги:**
- Не потребує Xcode
- Меньше залежностей
- Швидше білдить
- Краще для серверів/headless

### Опція 2: Swift CLI Alternative

Можна розбити проект на 2 частини:

```
Go CLI          (основний, це у нас вже є)
    ↓
Swift app       (опціонально, потребує Xcode)
```

### Опція 3: Встанови Повну Xcode

```bash
# Завантаж з App Store або:
# https://developer.apple.com/download

# Потім:
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
./build.sh --app
```

## 🎯 Мій Порадок

**CLI Tools + Go = Достатньо!**

```bash
./build.sh --cli --install

# Після цього у тебе буде:
# ✅ gatekeeper daemon        - фоновий процес
# ✅ gatekeeper status        - статус команда
# ✅ tmux integration         - для меню бару
# ✅ HTTP endpoints          - для моніторингу
```

**Якщо хочеш macOS app:**

```bash
# 1. Встанови Xcode з App Store
# 2. Запусти:
./build.sh --app
```

## 📊 Порівняння

| Функція | Go CLI | Swift App |
|---------|--------|-----------|
| Daemon | ✅ | ❌ |
| CLI commands | ✅ | ❌ |
| tmux status | ✅ | ❌ |
| HTTP API | ✅ | ❌ |
| MenuBar icon | ❌ | ✅ |
| Widgets | ❌ | ✅ |
| Потребує | CLI Tools | Xcode |
| Розмір | 5.9MB | ~100MB |

## 🚀 Висновок

**Для більшості користувачів Go CLI достатньо!**

```bash
./build.sh --cli
```

Це дає 80% функціональності без установки 15GB Xcode.

Якщо хочеш повно функціональності (MenuBar + Widgets), потрібна повна Xcode, але це опціонально.
