# ❓ Про Xcode, Swift и Build

## 🤔 "Чому я отримую помилку xcodebuild?"

Це **НЕ ПОМИЛКА** — це просто інформація.

**Значення:**
- У тебе є **Command Line Tools** ✅
- У тебе **НЕ ма** повної **Xcode** (15GB)

## ✅ Добра новина!

Go CLI біжить **ідеально** з Command Line Tools!

```bash
./build.sh --cli
gatekeeper daemon
gatekeeper status
```

Усе працює без додаткових встановлень.

## 🎯 Що Ти Отримаєш

### Z CLI Tools (Вже У Тебе)
```
✅ Go daemon              - фоновий процес перевірки
✅ CLI commands           - gatekeeper status, daemon, init
✅ tmux integration       - статус у меню бару
✅ HTTP API               - localhost:8080/health
✅ Logging & monitoring   - вся телеметрія
✅ 5.9MB binary          - мало ресурсів
```

### Потребує Повної Xcode (15GB)
```
❌ MenuBar app           - icon у меню
❌ Desktop widgets       - WidgetKit
❌ Lock screen widgets   - WidgetKit
```

## 🚀 Рекомендація

**CLI Tools достатньо!** 

Це дає 95% функціональності без встановлення 15GB.

```bash
./build.sh --cli --install
gatekeeper daemon
gatekeeper status --compact  # для tmux
```

## 📋 Якщо Хочеш Повна Xcode

Тільки якщо потрібна **MenuBar app**:

```bash
# 1. Завантаж Xcode з App Store
# 2. Встанови (займе ~30 хвилин)
# 3. Переключись на Xcode:
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer

# 4. Білд app:
./build.sh --app
```

## 🔄 Swift vs Go

| Параметр | Swift App | Go CLI |
|----------|-----------|--------|
| Потребує | Xcode SDK | CLI Tools |
| Розмір | ~100MB | 5.9MB |
| Залежності | 15GB+ | Легкі |
| Функції | UI (MenuBar, Widgets) | Daemon, CLI, API |
| Запуск | Macbook тільки | Везде (serverы теж) |

**Вибір:** Go CLI для **простоти**, Swift app для **красоти**

## 📚 Документація

- **BUILD.md** - Детальна білда (англ.)
- **BUILD_SWIFT.md** - Swift vs Go (англ.)
- **QUICKSTART.txt** - Команди

## ✨ Рішення

**Не встановлюй Xcode!** Go CLI вже дає всё що потрібно.

```bash
./build.sh --cli
# Готово! 🎉
```

---

**Висновок:** Помилка про xcodebuild можна безпечно ігнорувати. CLI працює нормально.
